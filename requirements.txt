# DSA Pattern Vault — Python toolchain (offline artifacts only; the web app lives in app/)

# Anki deck builder (anki/build_deck.py)
genanki>=0.13
PyYAML>=6.0

# Offline PDF builder (site/build_pdf.py)
markdown>=3.6
weasyprint>=61
